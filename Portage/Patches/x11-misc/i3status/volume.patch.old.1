--- a/i3status.conf	2019-11-07 14:43:51.627078592 -0500
+++ b/i3status.conf	2019-11-07 14:47:06.905083434 -0500
@@ -11,12 +11,14 @@
         interval = 5
 }
 
-order += "ipv6"
-order += "disk /"
+# order += "ipv6"
 order += "wireless _first_"
 order += "ethernet _first_"
+order += "disk /"
+# order += "load"
+order += "cpu_temperature 0"
 order += "battery all"
-order += "load"
+order += "volume master"
 order += "tztime local"
 
 wireless _first_ {
@@ -45,3 +47,13 @@
 disk "/" {
         format = "%avail"
 }
+
+volume master {
+	format = "%volume"
+	format_muted = "(%volume)"
+}
+
+cpu_temperature 0 {
+	format = "%degrees°C"
+	path = "/sys/devices/virtual/thermal/thermal_zone6/temp"
+}
